# Backlog

A running log of every review round this project has gone through — what each round
found, what it changed, and what it deliberately left alone. Appended to, never
regenerated or trimmed. Format and who writes each entry: see stage-0-supplax's
`references/backlog-format.md`.

## Round 1 of up to 4 — standard
Scope: увесь набір документів, згенерованих при старті проєкту
Found: документи в цілому узгоджені; один реальний розрив — PROMPTS.md посилався на рішення про візуальний стиль, якого PIPELINE.md насправді не фіксував.
Changed: додав це рішення в PIPELINE.md, щоб посилання з PROMPTS.md вело на реальний текст.
Rejected: не вибирав конкретний інструмент для бази даних (рішення ще не ухвалене), не чіпав контактні плейсхолдери в CODE_OF_CONDUCT.md/SECURITY.md (реального контакту ще немає).

## Round 2 of up to 4 — standard
Scope: увесь набір документів, згенерованих при старті проєкту
Found: `.gitignore` насправді не ігнорував папки з даними, хоча документація стверджувала протилежне; інструкція з тестування містила зламаний псевдокод замість чесного "ще не вирішено"; термін "Skill" використовувався у двох різних значеннях без пояснення.
Changed: виправив `.gitignore`, переписав інструкцію з тестування звичайним текстом, розвів два значення "Skill" у словнику термінів.
Rejected: залишив спірне формулювання в CONTRIBUTING.md як є (це записана відповідь з інтерв'ю), не додав інструкцію по локальному розгортанню (відкладено на пізніше).

## Round 3 of up to 4 — standard
Scope: увесь набір документів, згенерованих при старті проєкту
Found: план розвитку згадував зв'язки між автоматизаціями як поле реєстру, а опис архітектури про це поле забув.
Changed: додав опис зв'язків між автоматизаціями в ARCHITECTURE.md.
Rejected: інші дрібні пункти (контакти, інструкція розгортання) лишились відкритими — те саме рішення, що й раніше.

## Round 4 of up to 4 — standard
Scope: увесь набір документів, згенерованих при старті проєкту
Found: повторна перевірка не виявила нових розривів — усі попередні виправлення тримаються.
Changed: нічого — цей раунд нічого не зламав і нічого нового не знайшов.
Rejected: те саме, що й у попередніх раундах (контакти, інструкція розгортання лишились відкритими).

## 2026-09-01 — перевірка готовності до порталу автоматизацій
Scope: docs/functions.md, dashboard/SUMMARY.md, dashboard/functions.md
Found: docs/functions.md не згадував нову реєстрацію і роль "Автоматизатор" — застарів після того, як цю функціональність додали.
Changed: додав опис реєстрації та оновив права автоматизатора в docs/functions.md, перегенерував dashboard/SUMMARY.md і dashboard/functions.md, статус проєкту змінив на "Працює" — застосунок уже в проді.
Rejected: dashboard/ROI.md залишив без змін — реальних цифр по власному ROI дашборда ще немає.
