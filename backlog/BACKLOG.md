# Backlog

A running log of every review round this project has gone through — what each round
found, what it changed, and what it deliberately left alone. Appended to, never
regenerated or trimmed. Format and who writes each entry: see stage-0-supplax's
`references/backlog-format.md`.

## Round 1 of up to 4 — standard
Scope: full generated file set (see PIPELINE.md's file list)
Found: Re-read all 31 tracked files plus the actual repo listing; ran `verify_repo.py` (clean PASS — no CI/test-command mismatch, no .gitignore/stack mismatch, no unresolved [TODO]/[year]/[fullname] markers) and confirmed no package.json exists yet, consistent with the project's pre-code Stage 0 framing. Cross-file consistency was otherwise strong (ARCHITECTURE.md ↔ PIPELINE.md §2 ↔ ADR-0001 on stack; TESTING.md ↔ PIPELINE.md §4/§7 on the CI-gate judgment call; DEPLOYMENT.md ↔ PIPELINE.md §7 on environments; .env.example ↔ PIPELINE.md §5 on variables). One real contradiction found: PROMPTS.md (Stage 1) attributes the "airy, clean, icon-driven, Claude-like" visual-style decision to PIPELINE.md §1, but §1 never recorded it — a real interview answer that hadn't made it into the phase file that should hold it.
Changed: PIPELINE.md §1 (Intent & Scope) — added the sentence naming that visual direction as a starting point for Stage 1's design conversation, so PROMPTS.md's citation now points at content that actually exists.
Rejected: (1) ARCHITECTURE.md's Components section describing the API layer's GitHub/ClickUp metadata-lookup role without explicit Roadmap phase-gating — left alone, the Data Flow section already phases it correctly and this is just a fuller architectural description, not a contradiction. (2) Naming a specific ORM/DB-access tool — never decided in the interview; ARCHITECTURE.md's existing "structure intentionally not fixed yet" framing already covers it, and picking one now would invent an undecided decision. (3) CODE_OF_CONDUCT.md's and SECURITY.md's contact placeholders — genuine gaps but unresolvable without fabricating a name/email; left as honest placeholders for the human-touch list.

## Round 2 of up to 4 — standard
Scope: full generated file set (see PIPELINE.md's file list)
Found: Re-read all 31 tracked files plus the actual repo/folder listing (data/, notebooks/, reports/figures/, config/, tests/, infra/prod|staging/ with .gitkeep files), confirming the dashboard/analytics archetype folders from PIPELINE.md §2 were actually created. Ran `verify_repo.py`: clean, same pre-existing false-positive WARN as round 1 (BACKLOG.md's own round-1 entry contains the literal substring "[TODO]" in prose describing the placeholder check, not an actual unresolved marker — confirmed against verify_repo.py's regex, which only matches literal [TODO]/[FIXME]/[PLACEHOLDER]/[year]/[fullname] tokens). Fact-checked TESTING.md ↔ PIPELINE.md §4/§7, ARCHITECTURE.md ↔ PIPELINE.md §2/ADR-0001, and DEPLOYMENT.md ↔ PIPELINE.md §7 — all three still consistent, no contradiction. No package.json yet, still correct for pre-code Stage 0. ROI.md's hypothesis/measurement plan (owner-reported baseline at registration, capture mechanism honestly deferred to design stage) is still concrete enough for this stage and "Confidence: Estimated" still holds. Critical look surfaced three real, fixable gaps round 1 missed: (1) .gitignore never actually ignored data/{raw,interim,processed,external} despite data/README.md and the project-layout reference both claiming those folders are "typically gitignored" — a stranger following the documented convention would have their first real data file committed anyway. (2) TESTING.md's "How to Run" wrapped an HTML placeholder comment inside a ```bash fence, reading as broken pseudo-code rather than the honest "not decided yet" prose it was meant to be. (3) GLOSSARY.md's "Skill" entry covered only the registry-field sense, while ROADMAP.md/ARCHITECTURE.md use "a self-service skill" for an unrelated sense (a future registration tool for the project itself) — same word, two meanings, undocumented collision. Also confirmed CODE_OF_CONDUCT.md's and DEPLOYMENT.md's continued inclusion still correctly match file-set.md's conditional-inclusion criteria, despite reading momentarily odd against README's "internal team only" framing.
Changed: (1) .gitignore — added an explicit block ignoring data/{raw,interim,processed,external}/* (keeping each folder's .gitkeep), so the actual repo config matches what data/README.md and the project-layout convention claim. (2) TESTING.md — replaced the bash-fenced HTML-comment placeholder in "How to Run" with plain prose stating no test command exists yet and what it should become. (3) GLOSSARY.md — retitled the "Skill" entry to "Skill (registry field)" and added a sentence distinguishing it from ROADMAP.md/ARCHITECTURE.md's unrelated "self-service skill" sense, so the term collision is now documented instead of silently confusing.
Rejected: Left BACKLOG.md's round-1 "[TODO]" substring untouched — a false positive in verify_repo.py's checker, not a real placeholder, flagged here for awareness rather than fixed. Left CONTRIBUTING.md's "outside contributors are expected" framing as-is despite reading oddly next to "Proprietary"/"internal team only" elsewhere — it's a directly recorded Phase 3 interview answer that correctly triggered CODE_OF_CONDUCT.md's own inclusion rule, not an invented contradiction, and rewording it would risk overriding a real decision. Left CODE_OF_CONDUCT.md's and SECURITY.md's enforcement/contact placeholders as-is — genuine gaps, unresolvable without fabricating a name/email, same call round 1 made; still on the human-touch list. Did not add a Docker/local-Postgres setup guide to README.md/docs/getting-started.md — reasonably deferred to the Stage 1 design pass named in PIPELINE.md §1/PROMPTS.md, not a dodge. Left reports/figures/ out of the .gitignore fix — unlike data/, no file claims its contents are gitignored, so adding a rule there would invent a policy rather than fix a stated one.

## Round 3 of up to 4 — standard
Scope: full generated file set (see PIPELINE.md's file list)
Found: Re-read all 32 tracked files plus the actual repo/folder listing (data/, notebooks/, reports/figures/, config/, tests/, infra/staging|prod/ all present with expected README.md/.gitkeep contents; still no package.json, correct for pre-code Stage 0; still no .git directory, outside this doc-review's scope). Ran verify_repo.py: clean, same pre-existing false-positive WARNs as rounds 1-2 (BACKLOG.md's own prose literally containing "[TODO]"/"[FIXME]"/"[PLACEHOLDER]" while describing the checker, confirmed against the script's regex, not a real unresolved marker). Fact-checked the three most consequential cross-file claims — TESTING.md's CI-gate language vs. PIPELINE.md §4/§7, ARCHITECTURE.md's stack claim vs. PIPELINE.md §2/ADR-0001, DEPLOYMENT.md's "staging + prod" vs. PIPELINE.md §7 and infra/'s actual subfolders — all still consistent. README.md's npm commands remain honest against the pre-code state; ROI.md's "Confidence: Estimated" and its deferred measurement plan still hold, unchanged from rounds 1-2. Critical-look pass surfaced one real, previously-missed cross-file gap: ROADMAP.md's "Now" scope (which PIPELINE.md §1 anchors as v1's "done" definition) lists "related/dependent automations" and "description" as registry fields, but ARCHITECTURE.md's Postgres component bullet enumerated automations/skills/categories/ownership/ROI/links and silently dropped both — most importantly the relational field (a self-referencing link between automation records), which has real schema-design implications. Everything else checked (CODE_OF_CONDUCT.md/SECURITY.md placeholders, CONTRIBUTING.md's outside-contributors framing, FAQ.md's empty skeleton, TODO.md's intentional emptiness, the dashboard/analytics archetype folders, conditional-file inclusion criteria, .gitignore's Node template) remained consistent with rounds 1-2 and the reference rules — no new problems found.
Changed: ARCHITECTURE.md's Components section, Postgres database bullet — added "(including a free-text description)" after "automations" and added a clause noting relationships between automations (related/dependent automations, per ROADMAP.md's "Now" scope) imply a self-referencing link between automation records, not just flat rows. Brings the data-model description in line with the registry-field scope ROADMAP.md/PIPELINE.md §1 already committed to.
Rejected: Left CONTRIBUTING.md's "outside contributors" phrasing as-is — same call rounds 1-2 made, a directly recorded Phase 3 answer with no new evidence to justify relitigating it a third time. Left CODE_OF_CONDUCT.md's and SECURITY.md's contact placeholders untouched — genuine gaps, unresolvable without fabricating a name/email/channel, same human-touch item as rounds 1-2. Did not add a Docker/local-Postgres setup guide — still reasonably deferred to the Stage 1 design pass. Did not expand GLOSSARY.md's "Registry" entry to enumerate every field including the newly-added relational one — a glossary defines the concept, not the full schema, so a shorter description remains intentional. Did not initialize a git repository — out of scope for a documentation-content review; nothing in the doc set claims commits/branches already exist.

## Round 4 of up to 4 — standard
Scope: full generated file set (see PIPELINE.md's file list)
Found: Re-read all 32 tracked files plus the actual repo/folder listing (data/, notebooks/, reports/figures/, config/, tests/, infra/staging|prod/, docs/adr/ — all present with expected README.md/.gitkeep contents matching the Dashboard/analytics archetype; still no package.json, correct for pre-code Stage 0; still no .git directory, outside this doc-review's scope). Ran verify_repo.py: clean, same pre-existing false-positive WARNs as rounds 1-3 (BACKLOG.md's own prose literally contains "[TODO]"/"[FIXME]"/"[PLACEHOLDER]" while narrating what the checker looks for — confirmed against the script's regex, not a real unresolved marker; BACKLOG.md is out of scope for me to edit this round anyway). Fact-checked the three most consequential cross-file claims — TESTING.md's CI-gate language vs. PIPELINE.md §4/§7, ARCHITECTURE.md's stack claim vs. PIPELINE.md §2/ADR-0001, DEPLOYMENT.md's "staging + prod" vs. PIPELINE.md §7 and infra/'s actual subfolders — all three still consistent. Verified every in-repo markdown file-to-file reference (backtick mentions and `[]()` links across all 32 files) resolves to a real file, with no dangling references and no lingering mentions of intentionally-skipped files (RUNBOOK.md, docs/api.md, CODEOWNERS) as anything other than "not created." ROI.md's "Confidence: Estimated" still correctly reflects no automation onboarded and no code yet; its hypothesis and owner-reported measurement plan remain as concrete as the interview supports, unchanged from rounds 1-3 — only the literal UI/schema mechanics (never reached in the interview) stay open, honestly flagged as deferred rather than smoothed over. Critical-look pass (missing-context, self-contradiction, skeptical-contributor tests) surfaced no new cross-file contradiction, dropped answer, or vague dodge beyond what rounds 1-3 already caught and fixed — specifically re-checked and still consistent: conditional-file inclusion criteria, GLOSSARY.md's registry-field vs. self-service-skill disambiguation, .env.example vs. PIPELINE.md §5, ROADMAP.md's registry fields vs. ARCHITECTURE.md's Postgres bullet (round 3's fix holding), PROMPTS.md's Stage breakdown vs. ROADMAP.md's Now/Next/Later buckets, .gitignore's data/ block vs. data/README.md (round 2's fix holding), TESTING.md's "How to Run" prose (round 2's fix holding). One item re-considered but not changed: README.md's and docs/getting-started.md's npm install/dev instructions read as runnable today despite no package.json existing, while TESTING.md/OBSERVABILITY.md/DEPLOYMENT.md each carry an explicit "no code yet" caveat — on reflection not a new bug, since Install/Usage is inherently forward-looking per file-set.md's template design (the intended setup once the app exists), the same call rounds 1-3 made.
Changed: Nothing — no file was edited this round. Prior rounds' fixes (PIPELINE.md §1's visual-direction citation, the .gitignore data/ block, TESTING.md's placeholder prose, GLOSSARY.md's Skill disambiguation, ARCHITECTURE.md's Postgres bullet) had already resolved the real cross-file gaps; this round's re-read, fact-check, and critical-look passes found the file set internally consistent with no new substantive or cosmetic issue worth touching.
Rejected: README.md's/docs/getting-started.md's forward-looking npm install/dev instructions — left as-is; intentional per file-set.md's template design, same conclusion rounds 1-3 reached, and the docs describing *current* status already carry their own "no code yet" caveats where that distinction matters. GLOSSARY.md's "self-service skill" cross-reference to "ROADMAP.md/ARCHITECTURE.md's Data Flow" was checked for precision (only ARCHITECTURE.md's Data Flow uses that exact phrase) — left alone as a defensible reading, not a factual error, not worth a purely cosmetic edit. CODE_OF_CONDUCT.md's and SECURITY.md's contact placeholders remain untouched — same unresolvable human-touch gap as rounds 1-3, no real name/email/channel was ever given. backlog/BACKLOG.md's literal "[TODO]"/"[FIXME]"/"[PLACEHOLDER]" substrings (verify_repo.py WARN, same as rounds 1-3) were confirmed as a false positive against the checker's regex; per this task's instruction, backlog/BACKLOG.md was not touched by me at all — that's the scribe step's job.
