# src/

The project's own maintained code — not exploratory, not one-off. If a script in
`scripts/` or a notebook in `notebooks/` proves out an idea that needs to run
regularly, it graduates here, not the other way around.
